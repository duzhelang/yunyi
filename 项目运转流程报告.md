# 云医智护 — 糖尿病智能诊断与健康管理系统
## 项目运转流程报告

> 文档版本：v2.0 | 更新日期：2026-05-28 | 基于代码仓库实际状态编写

---

## 一、系统运行环境总览

### 1.1 服务架构

| 服务 | 技术栈 | 端口 | 说明 |
|------|--------|------|------|
| 前端服务 | Vue 3.4 + Vite 5 + Element Plus 2.4 | 5173 | SPA单页应用，开发热更新 |
| 后端服务 | Spring Boot 3.3.5 + MyBatis-Plus 3.5.9 | 9090 | RESTful API，JWT无状态认证 |
| 数据库 | MySQL 8.0 | 3306 | 25张业务表，UTF-8mb4编码 |
| 缓存 | Redis 6.2 + Spring Cache | 6379 | 系统配置缓存 |
| AI引擎 | Python 3.12 + PyTorch 2.11 | — | 子进程模式由后端按需调用 |
| AI大模型 | 智谱/DeepSeek/Kimi/MiMo | 443 | OpenAI兼容接口，HTTPS调用 |

### 1.2 项目目录结构

```
Software-ODA125/
├── src/main/java/com/oda/springboot/   # 后端Spring Boot代码
│   ├── controller/    30个Controller
│   ├── service/       业务逻辑层
│   ├── mapper/        MyBatis-Plus数据访问层
│   ├── entity/        实体类（25张表映射）
│   ├── config/        JWT拦截器、跨域、缓存配置
│   ├── utils/         UsePythonUtils等工具类
│   └── common/        统一响应、异常处理
├── vue/src/                              # 前端Vue3代码
│   ├── views/         15+页面组件
│   ├── components/    通用组件+health子组件
│   ├── composables/   15个组合式函数
│   ├── store/         Pinia状态管理
│   ├── router/        路由配置+动态路由
│   └── utils/         HTTP封装、缓存工具
├── python/                              # Python算法层
│   ├── train.py           模型训练
│   ├── incremental_train.py  增量训练
│   ├── predict.py         批量预测
│   ├── predict_single.py  单条预测（SHAP+MC Dropout）
│   ├── diabetes_chat.py   AI对话机器人
│   ├── read_data.py       数据读取
│   ├── data_analysis.py   数据分析
│   └── A10.py             数据处理+训练一体化
├── sql/                                 # 数据库初始化脚本
└── docs/                                # 项目文档中心（51个文件）
```

---

## 二、系统启动与运转流程

### 2.1 启动顺序

```
Step 1: MySQL数据库 → 确认25张表已初始化（执行sql/Software-ODA.sql）
Step 2: Redis缓存服务 → 可选，不启动则降级为无缓存模式
Step 3: Spring Boot后端 → mvn spring-boot:run（端口9090）
Step 4: Vue前端开发服务器 → cd vue && npm run dev（端口5173）
Step 5: Python依赖安装 → pip install -r requirements.txt（首次）
```

### 2.2 用户登录与认证运转流程

```
1. 用户访问 http://localhost:5173
2. 前端路由守卫(router.beforeEach)检查Token
   ├── Token存在且有效 → 跳转目标页面
   ├── Token不存在 → 重定向至Login.vue
   └── Token过期 → 清除本地存储，重定向登录
3. 用户提交登录表单 → POST /user/login
4. 后端UserController验证用户名密码
5. 签发JWT Token（Auth0 java-jwt 3.10.3）
   └── Token包含userId + 密码签名（防篡改）
6. 前端存储Token至localStorage
7. 请求sys_menu表 → 按角色获取菜单权限
8. router.addRoute动态添加路由
9. Aside.vue渲染侧边菜单（数据库驱动，零代码新增页面）
```

### 2.3 四角色RBAC权限模型运转

```
┌─────────────────────────────────────────────────────────────┐
│                    sys_user (15条预置用户)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │  管理员   │ │   医生   │ │ 普通用户  │ │  运维人员    │   │
│  │ (admin)  │ │(doctor)  │ │ (user)   │ │   (ops)     │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └─────┬───────┘   │
│       │            │            │              │           │
│  sys_role_menu 角色-菜单关联表（RBAC权限映射）               │
│       │            │            │              │           │
│  ┌────▼─────┐ ┌────▼─────┐ ┌────▼─────┐ ┌─────▼───────┐   │
│  │用户管理   │ │医生工作台 │ │风险快检   │ │工单处理      │   │
│  │角色配置   │ │待诊断列表 │ │健康档案   │ │日志记录      │   │
│  │模型训练   │ │个体洞察   │ │AI问诊     │ │工单统计      │   │
│  │系统配置   │ │群体分析   │ │每日打卡   │ │             │   │
│  │数据集管理  │ │诊疗记录   │ │就诊记录   │ │             │   │
│  │          │ │          │ │教育资源   │ │             │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心业务模块运转流程

### 3.1 糖尿病风险预测（核心功能）

#### 单条预测运转链路

```
前端HealthProfileView.vue
  → usePrediction()组合式函数
    → POST /api/predict/single
      → SinglePredictController.singlePredict()
        → SinglePredictServiceImpl.singlePredict()
          ├── validateInput()：校验8项特征值范围
          │   ├── Pregnancies: 0~20
          │   ├── Glucose: 0~300
          │   ├── BloodPressure: 0~200
          │   ├── SkinThickness: 0~100
          │   ├── Insulin: 0~900
          │   ├── BMI: 0~80
          │   ├── DiabetesPedigreeFunction: 0~3
          │   └── Age: 0~150
          ├── ModelVersionService.getActiveModelPaths()
          │   └── 查询sys_model_version表status='active'记录
          ├── UsePythonUtils.callPythonAndGetOutput()
          │   └── 执行: python predict_single.py <8个参数> --model --scaler --encoder
          │       └── 超时控制: 30秒
          └── parseJsonResponse()
              ├── prediction: 0/1（无/有糖尿病）
              ├── probability: 0~100%（患病概率）
              ├── risk_level: 低风险/中风险/高风险
              ├── confidence_interval: 95%置信区间（MC Dropout）
              ├── feature_importance: 各特征贡献度
              └── charts: SHAP图表Base64编码
```

#### Python推理引擎内部流程

```
predict_single.py
  ├── 加载模型（.pth）+ scaler + encoder
  ├── 数据预处理：特征缩放（StandardScaler）
  ├── 模型前向传播：三层全连接网络
  │   ├── fc1: 输入层 → 隐藏层1（ReLU + Dropout 0.2）
  │   ├── fc2: 隐藏层1 → 隐藏层2（ReLU + Dropout 0.2）
  │   └── fc3: 隐藏层2 → 输出层（Sigmoid）
  ├── MC Dropout不确定性估计（默认15次迭代）
  │   └── 保持Dropout开启，多次前向传播 → 均值±标准差 → 95%置信区间
  ├── SHAP特征重要性分析
  │   └── KernelExplainer → 特征贡献度排序 → 瀑布图/柱状图
  └── 输出JSON至stdout → 后端捕获解析
```

### 3.2 AI模型在线训练运转流程

```
管理员操作流程：
1. DatasetManagement页面 → 扫描/上传CSV训练集
   └── POST /api/dataset/scan → FileScanService扫描目录
       └── 自动入库sys_trainfile（质量等级/样本数/列信息）

2. OnlineTraining页面 → 创建训练任务
   └── POST /api/train-task/start
       ├── 选择训练集 + 配置超参数（lr/epochs/batch_size）
       └── TrainTaskService（@Async异步）

3. 后台训练执行：
   ├── 新建训练: train.py <csv> <output> [--lr] [--epochs] [--batch-size]
   ├── 增量训练: incremental_train.py <csv> <output> --base-model <path>
   │   └── 可选冻结层: none / fc1 / fc1+fc2 / all-hidden
   ├── 数据预处理: 缺失值填充 → StandardScaler标准化 → LabelEncoder编码
   ├── 模型训练: PyTorch三层全连接网络
   └── 输出: .pth模型文件 + scaler.pkl + encoder.pkl + 指标JSON

4. 训练结果自动入库：
   └── sys_model_version表
       ├── accuracy / loss / recall_rate / precision_rate / f1_score / auc
       ├── confusion_matrix（JSON）
       └── status = 'inactive'（待激活）

5. 管理员激活模型：
   └── POST /api/model/{id}/activate
       └── 先将其他版本置为inactive → 再将目标版本置为active
```

### 3.3 AI智能问诊运转流程

```
DiabetesChat.vue
  ├── useChat()组合式函数
  │   ├── 消息列表管理（user/assistant/system）
  │   ├── 打字机效果（逐字渲染）
  │   └── 自动滚动到底部
  ├── useModelConfig()组合式函数
  │   ├── 识别用户角色（管理员可切换模型）
  │   └── GET /api/system/defaultModel（公开接口，无需Token）
  └── useTTS()组合式函数
      └── POST /api/diabetes/tts（MiMo音色定制）

对话流程：
1. 用户输入健康问题
2. POST /api/diabetes/chat
3. DiabetesController
   ├── 读取sys_config表获取默认大模型provider+api_key
   ├── 调用diabetes_chat.py <provider> <api_key> <question>
   └── Python脚本通过OpenAI兼容SDK调用远程大模型
       ├── 智谱GLM-4（zhipuai/glm-4-flash）
       ├── DeepSeek（deepseek-chat）
       ├── Kimi（moonshot-v1-8k）
       └── MiMo（MiMo-7B-Chat）
4. 流式响应SSE返回 → 前端打字机效果渲染
5. 强身份约束：糖尿病专科医生角色扮演

特殊功能：
- 上下文携带：从预测结果页跳转时，自动携带预测数据作为上下文
- TTS语音播报：POST /api/diabetes/tts → 适配老年用户
- 多模型切换：管理员可在系统配置中切换默认AI模型
```

### 3.4 全周期健康管理运转流程

```
HealthProfileView.vue（主页面，拆分为13个子组件）
├── WelcomeBanner.vue          欢迎横幅
├── HealthFormCard.vue         健康信息表单（11项指标+生活方式）
├── HealthAdviceCard.vue       AI个性化建议展示
├── HealthChartCard.vue        趋势图表（血糖/血压/体重/DPF）
├── HealthHistoryCard.vue      历史档案列表
├── HealthToolCard.vue         工具入口
├── DailyCheckinCard.vue       每日打卡（饮食/运动/用药/血糖）
├── DpfInfoDialog.vue          DPF说明弹窗
├── ResultDialog.vue           预测结果弹窗
├── PlanGeneratingOverlay.vue  健康计划生成加载遮罩
└── tools/
    ├── EmergencyToolDialog.vue  急救指南
    ├── CarbCalculatorDialog.vue 碳水计算器
    └── FootCareDialog.vue      足部护理

核心Composables（组合式函数）：
- useHealthForm: 表单业务逻辑 + 数据校验
- usePrediction: 预测封装（缓存+本地回退）
- useHealthChart: ECharts图表渲染
- useHealthHistory: 历史记录管理
- useDailyCheckin: 每日打卡逻辑
- useHealthPlan: 健康计划生成（风险评估+一周计划）
- useRecipe: 控糖食谱生成（饮食偏好+安全HTML处理）
- useDraftPersistence: 草稿数据本地持久化
- useHealthValidator: 健康数据验证规则

数据流转：
1. 用户填写健康档案 → useHealthForm表单校验
2. POST /api/health-profile/save-and-predict
3. 后端写入user_health_profiles表 + 同步AI预测
4. AI生成个性化健康建议（ai_advice字段）
5. 可选发送给医生（status: PENDING → DONE）
6. 每日打卡记录 → treatment_checkin表
7. ECharts趋势图可视化历史数据
```

### 3.5 诊断服务运转流程（医生端）

```
DoctorWorkbench.vue（医生工作台）
  ├── 待诊断列表
  │   └── GET /api/health-profile/doctor/pending
  │       └── 查询status='PENDING'的健康档案
  ├── 查看患者详情
  │   └── GET /api/health-profile/{id}
  ├── 下载诊断CSV
  │   └── GET /api/health-profile/download-csv/{id}
  ├── 提交诊断结果
  │   └── POST /api/health-profile/doctor/submit-result
  │       └── 更新status='DONE' + diagnosis_result + diagnose_time
  └── 群体分析
      └── POST /api/analysis/group
          └── 批量预测 + 分布/聚类/相关性分析

IndividualInsight.vue（个体洞察）
  ├── GET /api/insight/{healthProfileId}
  │   ├── SHAP归因分析（特征贡献度排序）
  │   ├── 相似病例匹配（余弦相似度）
  │   └── 历史健康趋势
  └── ECharts渲染：瀑布图/雷达图/趋势折线图

GroupAnalysis.vue（群体分析）
  ├── POST /api/analysis/group
  │   ├── 年龄分布直方图
  │   ├── 性别/风险占比饼图
  │   ├── BMI-血糖散点图
  │   └── 风险热力图
  └── analysis_record表持久化（支持历史查询）
```

### 3.6 维修工单运转流程

```
完整生命周期：

用户提交报修 → pending
  └── POST /api/repair/submit
      └── sys_repair_order表（故障类型/紧急程度/附件/联系方式）

运维管理员分配 → assigned
  ├── PUT /api/repair/assign/{id}（管理员指派）
  └── PUT /api/repair/claim/{id}（维修员认领）

维修员处理 → processing
  └── PUT /api/repair/process/{id}
      └── sys_repair_log表记录处理日志

工单完成 → completed
  └── PUT /api/repair/confirm/{id}（用户确认）

用户评价 → closed
  └── PUT /api/repair/evaluate/{id}
      └── sys_repair_evaluation表（1-5星评分）

异常路径：
  └── PUT /api/repair/reopen/{id}（不满意重新打开 → processing）

统计分析：
  └── GET /api/repair/stats（处理时长/满意度/类型分布）
```

---

## 四、数据层运转机制

### 4.1 数据库25张表分布

| 分类 | 表名 | 说明 |
|------|------|------|
| 用户权限 | sys_user, sys_role, sys_role_menu, sys_menu | 4张 |
| 系统管理 | sys_config, sys_dict, sys_message | 3张 |
| 健康数据 | user_health_profiles, prediction_record, diabetes_record, sys_result, analysis_record | 5张 |
| 诊疗相关 | sys_treatment_record, treatment_checkin, patient_visit_record | 3张 |
| AI模型训练 | sys_trainfile, sys_testfile, sys_train_task, sys_model_version | 4张 |
| 教育资源 | diabetes_education, diabetes_video, education_comment | 3张 |
| 维修工单 | sys_repair_order, sys_repair_log, sys_repair_evaluation | 3张 |

### 4.2 关键数据表字段亮点

- **user_health_profiles**：11项生理指标 + 8项生活方式数据 + prediction_json（JSON字段存储完整预测结果） + ai_advice（AI个性化建议）
- **sys_train_task**：6项评估指标（accuracy/loss/recall/precision/f1/auc） + confusion_matrix（JSON） + hyper_params（JSON超参数）
- **sys_model_version**：file_path + scaler_path + encoder_path三路径管理 + metrics（JSON性能指标） + status激活状态
- **sys_repair_order**：完整报修信息 + attachments（JSON附件数组） + 4种状态流转

### 4.3 数据保护机制

- 软删除：patient_visit_record.is_deleted字段
- 自动淘汰：诊疗档案保留最近5次，健康档案保留30条
- JSON字段存储：图表Base64、生活方式数据、性能指标等复杂数据统一JSON格式
- 数据隔离：通过JWT Token提取userId，用户只能查看自己的数据

---

## 五、Python算法层运转机制

### 5.1 调用方式

后端通过 `UsePythonUtils` 工具类以子进程模式调用Python脚本：

```java
// 核心调用方法
UsePythonUtils.callPythonAndGetOutput(scriptPath, args...)  // 捕获stdout
UsePythonUtils.callPython(scriptPath, args...)               // 获取退出码
UsePythonUtils.callPythonWithCallback(scriptPath, callback)  // 带回调

// 自动配置
- UTF-8编码环境变量
- 30秒超时控制（单条预测）
- Python解释器路径：PropertyUtil.pythonExe
```

### 5.2 模型架构

```
三层全连接神经网络（DiabetesNet）
├── fc1: 8 → 64（ReLU + Dropout 0.2）
├── fc2: 64 → 32（ReLU + Dropout 0.2）
└── fc3: 32 → 1（Sigmoid）

输入特征：8项（Pregnancies, Glucose, BloodPressure, SkinThickness, Insulin, BMI, DPF, Age）
输出：0~1概率值（Sigmoid激活）

训练配置（默认）：
- 学习率：0.001
- 训练轮次：100
- 批大小：32
- 测试集比例：0.2
- 优化器：Adam
- 损失函数：BCELoss
```

### 5.3 增量训练特有能力

```
incremental_train.py
├── 加载已有模型权重（--base-model）
├── 复用预处理器（--reuse-preprocessor）
├── 冻结层级选择（--freeze-layers）
│   ├── none: 全部可训练
│   ├── fc1: 冻结第一层
│   ├── fc1+fc2: 冻结前两层
│   └── all-hidden: 冻结所有隐藏层
└── 适合小样本微调场景
```

---

## 六、前端架构运转机制

### 6.1 技术栈版本锁定

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4.0 | Composition API + SFC |
| Vite | 5.0+ | 构建工具，HMR热更新 |
| Element Plus | 2.4.4 | UI组件库 |
| ECharts | 5.6.0 | 数据可视化（9种图表） |
| Pinia | 2.1.7 | 状态管理（MainStore + HealthStore） |
| Vue Router | 4.2.0 | 路由管理 + 动态路由 |
| Axios | 1.6.0 | HTTP请求封装 |

### 6.2 组合式函数（Composables）体系

```
useHealthForm     → 健康表单核心逻辑（字段管理/提交/重置）
usePrediction     → 预测功能封装（缓存策略/本地回退）
useHealthChart    → ECharts图表渲染（主题配置/数据绑定）
useHealthHistory  → 历史记录管理（分页/筛选/删除）
useDailyCheckin   → 每日打卡（4项任务/进度追踪）
useHealthPlan     → 健康计划生成（风险评估/一周计划/Mock回退）
useRecipe         → 控糖食谱（饮食偏好/食谱解析/安全HTML）
useChat           → 智能对话核心（消息管理/打字机/自动滚动）
useTTS            → 语音合成（文本转语音/播放控制/静音切换）
useModelConfig    → AI模型配置（角色识别/默认模型/管理员设置）
useMarquee        → 跑马灯动画（垂直滚动/暂停恢复/循环）
useDraftPersistence → 草稿持久化（localStorage自动保存）
useHealthValidator  → 健康数据验证（范围/格式/必填规则）
useHealthTools    → 工具箱（急救/碳水/足部/DPF计算器）
useGlobalSound    → 全局音效管理
```

### 6.3 路由与权限运转

```
路由守卫执行流程：
router.beforeEach(to, from, next)
  ├── 1. 检查to.meta.requireAuth
  ├── 2. 从localStorage获取Token
  ├── 3. Token不存在 → next('/login')
  ├── 4. Token存在 → 发送请求验证有效性
  ├── 5. 首次加载 → 请求用户菜单权限
  ├── 6. 动态添加路由（router.addRoute）
  └── 7. next()放行

菜单数据来源：
  sys_menu表（30条记录）
  ├── pid字段实现树形结构
  ├── path字段映射前端路由
  ├── page_path字段映射组件路径
  ├── icon字段映射Element Plus图标
  └── sort_num字段控制排序
```

---

## 七、API接口运转统计

### 7.1 接口分布（30个Controller · 169个API）

| Controller | 接口数 | 核心功能 |
|------------|--------|----------|
| UserController | 16 | 用户CRUD、登录注册、角色查询、Excel导入导出 |
| SinglePredictController | 1 | 单条预测（核心） |
| PredictController | 3 | 批量预测、结果查询 |
| HealthProfileController | 11 | 健康档案CRUD、预测、医生诊断 |
| TreatmentRecordController | 7 | 诊疗档案CRUD、Excel导入导出 |
| TreatmentCheckinController | 2 | 打卡记录 |
| PatientVisitController | 8 | 就诊记录CRUD、图表数据、批量保存 |
| AnalysisController | 4 | 群体分析、历史记录 |
| InsightController | 2 | 个体洞察 |
| DetailbordController | 5 | 预测结果管理 |
| EchartsController | 7 | 数据可视化统计 |
| ModelController | 6 | 模型文件管理 |
| ModelVersionController | 9 | 模型版本管理、激活 |
| TrainTaskController | 5 | 训练任务管理 |
| DatasetController | 11 | 数据集管理、扫描上传 |
| DiabetesController | 2 | AI对话、TTS |
| RepairController | 16 | 维修工单全流程 |
| MenuController | 8 | 菜单管理 |
| RoleController | 8 | 角色管理、菜单绑定 |
| 其他Controller | 36 | 配置、消息、文件、科普、视频等 |

### 7.2 认证与安全

- JWT认证：Auth0 java-jwt 3.10.3
- Token签名：用户密码参与签名，修改密码自动失效旧Token
- 拦截器：JwtInterceptor统一校验，排除登录/注册/公开配置接口
- API密钥：环境变量注入，消除硬编码
- 数据隔离：通过Token提取userId，查询条件自动过滤

---

## 八、部署与运维

### 8.1 开发环境启动

```bash
# 后端
cd src/main/java
mvn spring-boot:run    # 端口9090

# 前端
cd vue
npm install
npm run dev            # 端口5173

# Python依赖（首次）
cd python
pip install -r requirements.txt
```

### 8.2 生产环境构建

```bash
# 后端打包
mvn clean package -DskipTests
java -jar target/Software-ODA-0.0.1-YUNYI.jar

# 前端构建
cd vue
npm run build          # 输出dist/目录

# 部署：Nginx反向代理 + Docker容器化
```

### 8.3 API文档

```
SpringDoc OpenAPI 3（springdoc-openapi-starter-webmvc-ui 2.2.0）
访问地址：http://localhost:9090/swagger-ui/index.html
```

---

## 九、版本与更新记录

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| v1.0 | 初始版本 | Vue2 + Spring Boot基础框架 |
| v1.3 | 2026-05-15 | Vue3全量重构，体积减少87% |
| v2.0 | 2026-05-28 | 新增MLOps模型管理、多模型AI编排、维修工单闭环 |

---

*本报告基于代码仓库实际代码和文档编写，反映系统真实运转状态。*
